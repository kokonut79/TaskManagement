@model MVC.Models.Tasks.IndexVM

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<form class="d-flex mt-3" role="search" action="/Task/Index">
    @Html.HiddenFor(m => m.Pager.ItemsPerPage)
    @Html.TextBoxFor(m => m.Filter.Title, new { placeholder = "Search by  Title ", @class = "form-control me-2" })
    <button class="btn btn-outline-success" type="submit" value="Filter">Search</button>
</form>

<br />

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            
        </th>
        <th>
            Title
        </th>
        <th>
            Description
        </th>
        <th>
            Status
        </th>
        <th>
            WorkerId
        </th>
       
        <th></th>
    </tr>

    @foreach (var item in Model.Items)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TaskId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Status)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.WorkerId)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.TaskId }) |
                @Html.ActionLink("Details", "Details", new { id = item.TaskId }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.TaskId })
            </td>
        </tr>
    }

</table>

<div class="row pager">
    <div class="col-3">
        @{
            Html.RenderPartial("/Views/Partial/PagerPartial.cshtml", Model.Pager);
        }
    </div>
</div>